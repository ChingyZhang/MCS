@{
    ViewBag.Title = "Index";
    //  Layout = "~/Views/_ViewStart.cshtml";
}


<table id="dg" title="My Users" class="easyui-datagrid" style="width: 100%; height: 250px"
    url="get_users.php"
    toolbar="#toolbar" pagination="true"
    rownumbers="true" fitcolumns="true" singleselect="true">
    <thead>
        <tr>
            <th field="firstname" width="50">表名</th>
            <th field="lastname" width="50">显示名称</th>
            <th field="phone" width="50">可扩展字段</th>
            <th field="email" width="50">Model实体名称</th>
            <th field="email" width="50">树形结构标志</th>
            <th field="email" width="50"></th>
            <th field="email" width="50"></th>
        </tr>
    </thead>
</table>
<div id="toolbar">
    <a href="javascript:void(0)" class="easyui-linkbutton" iconcls="icon-add" plain="true" onclick="newTable()">添加新表</a>
    <a href="javascript:void(0)" class="easyui-linkbutton" iconcls="icon-edit" plain="true" onclick="editUser()">编辑表</a>
    <a href="javascript:void(0)" class="easyui-linkbutton" iconcls="icon-remove" plain="true" onclick="destroyUser()">移除表</a>
</div>


<style type="text/css">
    #fm {
        margin: 0;
        padding: 10px 30px;
    }

    .ftitle {
        font-size: 14px;
        font-weight: bold;
        padding: 5px 0;
        margin-bottom: 10px;
        border-bottom: 1px solid #ccc;
    }

    .fitem {
        margin-bottom: 5px;
    }

        .fitem label {
            display: inline-block;
            width: 120px;
        }

        .fitem input {
            width: 150px;
        }
</style>

<div id="dlg" class="easyui-dialog" title="添加新表" style="width: 400px; height: 280px; padding: 10px 20px"
    closed="true" buttons="#dlg-buttons">
    <form id="fm" method="post" novalidate>
        <div class="fitem">
            <label>表名:</label>
            <input name="firstname" class="easyui-textbox" required="true">
        </div>
        <div class="fitem">
            <label>显示名称:</label>
            <input name="lastname" class="easyui-textbox" required="true">
        </div>
        <div class="fitem">
            <label>是否含扩展字段:</label>
            <select id="selExt" style="width: 150px"></select>
            <div id="spExt">
                <div style="color: #99BBE8; background: #fafafa; padding: 5px;">请选择</div>
                <div style="padding: 10px">
                    <input type="radio" name="rExt" value="0"><span>否</span><br />
                    <input type="radio" name="rExt" value="1"><span>是</span><br />
                </div>
            </div>
            <script type="text/javascript">
                $(function () {
                    $('#selExt').combo({
                        required: true,
                        editable: false,
                        panelHeight: 100
                    });
                    $('#spExt').appendTo($('#selExt').combo('panel'));
                    $('#spExt input').click(function () {
                        var v = $(this).val();
                        var s = $(this).next('span').text();
                        $('#selExt').combo('setValue', v).combo('setText', s).combo('hidePanel');
                    });
                });
            </script>
        </div>
        <div class="fitem">
            <label>Model名称:</label>
            <input name="email" class="easyui-textbox" required="true">
        </div>
        <div class="fitem">
            <label>是否为树形结构:</label>
            <select id="selTree" style="width: 150px"></select>
            <div id="spTree">
                <div style="color: #99BBE8; background: #fafafa; padding: 5px;">请选择</div>
                <div style="padding: 10px">
                    <input type="radio" name="rTree" value="0"><span>否</span><br />
                    <input type="radio" name="rTree" value="1"><span>是</span><br />
                </div>
            </div>
            <script type="text/javascript">
                $(function () {
                    $('#selTree').combo({
                        required: true,
                        editable: false,
                        panelHeight:100
                    });
                    $('#spTree').appendTo($('#selTree').combo('panel'));
                    $('#spTree input').click(function () {
                        var v = $(this).val();
                        var s = $(this).next('span').text();
                        $('#selTree').combo('setValue', v).combo('setText', s).combo('hidePanel');
                    });
                });
            </script>
        </div>
    </form>
</div>
<div id="dlg-buttons">
    <a href="javascript:void(0)" class="easyui-linkbutton c6" iconcls="icon-ok" onclick="saveUser()" style="width: 90px; flex-align: center;">Save</a>
    <a href="javascript:void(0)" class="easyui-linkbutton" iconcls="icon-cancel" onclick="javascript:$('#dlg').dialog('close')" style="width: 90px">Cancel</a>
</div>

<script type="text/javascript">

    var url;
    function newTable() {
        $('#dlg').dialog('open').dialog('setTitle', '添加新表');
        $('#fm').form('clear');
        url = 'save_user.php';
    }
    function editUser() {
        var row = $('#dg').datagrid('getSelected');
        if (row) {
            $('#dlg').dialog('open').dialog('setTitle', 'Edit User');
            $('#fm').form('load', row);
            url = 'update_user.php?id=' + row.id;
        }
    }
    function saveUser() {
        alert("saveuser func");
        $('#fm').form('submit', {
            url: url,
            onSubmit: function () {
                return $(this).form('validate');
            },
            success: function (result) {
                var result = eval('(' + result + ')');
                if (result.errorMsg) {
                    $.messager.show({
                        title: 'Error',
                        msg: result.errorMsg
                    });
                } else {
                    $('#dlg').dialog('close');        // close the dialog
                    $('#dg').datagrid('reload');    // reload the user data
                }
            }
        });
    }
    function destroyUser() {
        var row = $('#dg').datagrid('getSelected');
        if (row) {
            $.messager.confirm('Confirm', 'Are you sure you want to destroy this user?', function (r) {
                if (r) {
                    $.post('destroy_user.php', { id: row.id }, function (result) {
                        if (result.success) {
                            $('#dg').datagrid('reload');    // reload the user data
                        } else {
                            $.messager.show({    // show error message
                                title: 'Error',
                                msg: result.errorMsg
                            });
                        }
                    }, 'json');
                }
            });
        }
    }
</script>
